@model IEnumerable<Sous_Cloud_Pantry.models.Measurement>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MeasurementMethod)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MeasurementMethod)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MeasurementId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MeasurementId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MeasurementId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
